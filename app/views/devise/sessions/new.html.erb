<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
  <div class="form-floating">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>









 <div class="d-flex align-items-center py-4 bg-body-tertiary">
    <main class="form-signin w-100 m-auto text-center modal-content rounded-4 shadow">
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: {data: { turbo: :false } }) do |f| %>
        <svg xmlns="http://www.w3.org/2000/svg" width="200" height="200" fill="currentColor" class="mb-4 bi bi-person-badge" viewBox="0 0 16 16">
          <path d="M6.5 2a.5.5 0 0 0 0 1h3a.5.5 0 0 0 0-1h-3zM11 8a3 3 0 1 1-6 0 3 3 0 0 1 6 0z"/>
          <path d="M4.5 0A2.5 2.5 0 0 0 2 2.5V14a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V2.5A2.5 2.5 0 0 0 11.5 0h-7zM3 2.5A1.5 1.5 0 0 1 4.5 1h7A1.5 1.5 0 0 1 13 2.5v10.795a4.2 4.2 0 0 0-.776-.492C11.392 12.387 10.063 12 8 12s-3.392.387-4.224.803a4.2 4.2 0 0 0-.776.492V2.5z"/>
        </svg>
        <div class="form-floating">
          <input type="text" class="form-control" id="username floatingInput" placeholder="User-name" maxlength="100" name="username">
          <label for="username floatingInput" class="username">User Name</label>
        </div>
        <div class="form-floating">
          <input maxlength="25" name="password" type="password" class="form-control" id="password floatingPassword" placeholder="Password">
          <label for="floatingPassword password" class="password">Password</label>
        </div>
        <div class="form-check text-start my-3">
          <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault">
          <label class="form-check-label" for="flexCheckDefault">
            Remember me
          </label>
        </div>
        <button class="btn btn-outline-primary w-100 py-2" type="submit">Login</button>
        <hr>
        <button class="btn btn-outline-primary w-100 py-2" type="submit">Create new account</button>
        <div id="flash-message">
          <% if flash[:notice] %>
            <p class="btn btn-outline-danger"><%= flash[:notice] %></p>
          <% end %>
        </div>
        <p class="mt-5 mb-3 text-body-secondary">&copy; 2023</p>
      <% end %>

      <div class="position-fixed bottom-0 end-0 mb-3 me-3">
        <a class="btn btn-outline-primary py-2 d-flex align-items-center" href="/" role="button" aria-label="Back to Home">
          Back Home
        </a>
      </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </div>
